certManager:
  # DNS Provider: Google CloudDNS
  # Docs: https://onepanelio.github.io/core-docs/docs/deployment/configuration/tls#google-clouddns
  # CLI flag: --dns-provider=clouddns
  clouddns:
    projectId:
      required: true
      default: <project-id>
    serviceAccountKey:
      required: true
      default: <key.json-file-data>